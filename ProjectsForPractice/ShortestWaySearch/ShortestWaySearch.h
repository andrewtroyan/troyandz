#ifndef SHORTESTWAYSEARCH_H_INCLUDED
#define SHORTESTWAYSEARCH_H_INCLUDED

#include <stdio.h>
#include <stdlib.h>

typedef struct AlliedNode_
{
    int node, weigthOfEdge;
} AlliedNode;

AlliedNode *ListOfAlliedNodes;

#endif // SHORTESTWAYSEARCH_H_INCLUDED
